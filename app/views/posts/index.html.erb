<h5>新着投稿一覧</h5>
<div class="row"> 
  <div class="col s12">
    <% @posts.each do |post| %>
      <div>
      <%= render partial: "card", locals: { post: post } %>
      </div>
    <% end %>
  <div class="col s12">
    <div class="center">
      <%= paginate @posts %>
    </div>
  </div>
</div>
